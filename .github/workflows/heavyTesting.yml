name: Heavy testing/*

on:
  pull_request:
    types: ["closed"]
    branches:
      - main
jobs:
  heavy-testing:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Run heavy tests - Scenario 1
        run: python3 tests/run-all-tests.py