name: Light testing

on:
  pull_request:
    types: ["closed"]
    branches:
      - main
  workflow_dispatch:

jobs:
  light-testing:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Run server
        run: cargo run > /dev/null 2>&1 & sleep 60 && cd example/tests/ && python3 propertybased.py && pkill simeis